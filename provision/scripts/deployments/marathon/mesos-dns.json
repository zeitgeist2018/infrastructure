{
  "cmd": "cd /home/marathon/mesos-dns && sudo ./mesos-dns -config=config.json",
  "cpus": 1.0,
  "mem": 128,
  "id": "mesos-dns",
  "instances": 1,
  "privileged": true,
  "constraints": [
    [
      "hostname",
      "CLUSTER",
      "192.168.1.110"
    ]
  ]
}
